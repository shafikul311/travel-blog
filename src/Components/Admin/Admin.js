import React from 'react';
import NavBar from '../NabBar/NavBar';

const Admin = () => {
    return (
        <div>
            <NavBar />
            <h1>Admin</h1>
        </div>
    );
};

export default Admin;